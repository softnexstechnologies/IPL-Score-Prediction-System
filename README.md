# IPL-Score-Prediction-System
An AI-powered IPL Score Prediction System that predicts the final score of an IPL match in real time using Machine Learning, Deep Learning, and live match parameters. The project demonstrates an end-to-end ML workflow, from data preprocessing and model training to deployment using Streamlit.
